# Continuous Evaluation Benchmark Template: Knowledge Graph CLI Products

> **Purpose:** This document defines the standard evaluation matrix, execution rules, and baseline metrics for comparing Docuvia2 against sibling and competitor products (GitNexus, Graphify, Code-Review-Graph). Future AI agents can use this template to re-run the benchmark and compare regressions or improvements over time.

---

## 1. Benchmark Execution Rules

When an AI agent is instructed to "run the CLI benchmark", it MUST strictly adhere to the following steps:

0. **Isolation (mandatory, added after the 2026-07-13 run below caused real data loss — see §2b/§3 "Note on GitNexus"):** NEVER run another product's CLI (`gitnexus`, `graphify`, `code-review-graph`, …) directly inside this repo's primary working tree. First create a disposable git worktree — e.g. `git worktree add ../docuvia2-benchmark HEAD` — and run every non-Docuvia2 tool from inside that worktree instead. Remove it afterward with `git worktree remove ../docuvia2-benchmark`. Docuvia2's own CLI may still be run in the primary tree as usual.
   - ⚠️ **Caveat discovered 2026-07-22 (see §2b "Note on this session's worktree incident"): a git worktree of _Docuvia2 itself_ is NOT a safe place to run Docuvia2's own `snapshot`/`hydrate`/`sync-knowledge`.** Worktrees share the same underlying `.git` object/ref store as the primary tree, so packing a snapshot from inside a Docuvia2 self-worktree overwrites the _same_ `docuvia-knowledge` branch the primary tree uses — there is no per-worktree isolation for git refs the way there is for the working directory or `.docuvia/local.db`. When benchmarking Docuvia2's own build/query/impact/export-topology commands, a self-worktree is fine (and still recommended, to avoid touching the primary tree's real `.docuvia/local.db`); just skip `snapshot`/`hydrate`/`sync-knowledge` there, or immediately restore `docuvia-knowledge` to its pre-benchmark commit afterward (`git update-ref refs/heads/docuvia-knowledge <prior-sha>`, found via `git reflog show docuvia-knowledge`).
1. **Environment Preparation**:
   - `Docuvia2`: `pnpm install && pnpm run build`. Run via `npx tsx artifacts/cli/src/cli.ts <cmd>` or `node artifacts/cli/dist/cli.js <cmd>`.
   - `GitNexus`: from the isolated worktree (see §0), `npx gitnexus@latest <cmd>` or `node .gitnexus/run.cjs`.
   - `Graphify`: from the isolated worktree, `python -m venv .venv && source .venv/Scripts/activate && pip install -e .` then `graphify <cmd>`.
   - `Code-Review-Graph`: from the isolated worktree, `python -m venv .venv && source .venv/Scripts/activate && pip install -e .` then `code-review-graph <cmd>`.
2. **Measurement**: Use the `time` command in Bash (e.g., `time npx gitnexus status`) to capture real-world wall-clock latency for the user.
3. **Data Integrity**: The agent MUST NOT hallucinate capabilities. If a tool fails to build or a command is missing, log it as `N/A` or `Failed`. **A command that runs fast but returns empty/no-op results MUST be reported as such — speed alone does not indicate a functioning feature.**
4. **Output Format**: Overwrite the "Latest Run Results" sections below while preserving this overarching template structure.
5. **Safety**: Prefer running indexing/query/build commands against a real target repo (this benchmark uses Docuvia2's own working tree, copied into the §0 worktree, as the common target so all four tools are measured against identical input). Avoid running `install`/`uninstall`/`init` hook-registration commands live where they are known to mutate global, cross-project config (see §2a) — verify those via `--help`/source inspection instead and label the result "not executed live."

---

## 2. Latest Run Results (Date: 2026-07-22, re-verified with live command execution)

**Verification method:** The GitNexus/Graphify/Code-Review-Graph numbers below were produced by actually building/installing each tool and running the listed command against the real Docuvia2 repository (420 files, TypeScript/pnpm monorepo) as the common target, so the comparison is apples-to-apples; they were not re-executed in this pass (no code change on their side motivated re-running foreign CLIs). **The Docuvia2 numbers were fully re-verified in a second pass later the same day**, specifically because the bugs this document previously reported for Docuvia2 (§2b) no longer reproduced against current source — see the corrected findings below. That re-verification used a disposable git worktree (`git worktree add ../docuvia2-benchmark HEAD`, removed afterward) with its own `pnpm install`/`pnpm run build`, so it never touched the primary tree's real, accumulated `.docuvia/local.db` (2,852 L2 nodes from actual dogfooding at time of writing) or its `docuvia-knowledge` branch — except for one caught-and-reverted incident, see §2b. Two commands (`docuvia uninstall`, `docuvia init`) have real side effects on shared/global state — see the callouts in §2a; both were verified via source inspection this pass rather than executed live again.

### 2a. Category: Installation & Editor Hooks Integration

| Feature / Metric         | Docuvia2          | GitNexus                                                              | Graphify                                             | Code-Review-Graph (CRG)            |
| :----------------------- | :---------------- | :-------------------------------------------------------------------- | :--------------------------------------------------- | :--------------------------------- |
| **Initial Setup / Hook** | `init`            | `setup`                                                               | `install [--platform P]`                             | `install` / `init`                 |
| **Hook Teardown**        | `uninstall`       | `uninstall`                                                           | `uninstall`                                          | `unregister` (registry only)       |
| **Diagnostic & Health**  | `doctor`          | `doctor`                                                              | _N/A (no diagnose command found)_                    | `status` (stats, verified working) |
| **MCP Server Start**     | `mcp`             | `serve`, `mcp`                                                        | _N/A (no dedicated MCP subcommand seen in `--help`)_ | `serve`, `mcp`                     |
| **Live execution**       | See callout below | Not executed live (would mutate global editor config on this machine) | Not executed live (same reason)                      | Not executed live (same reason)    |

**Verified findings (2026-07-22):**

- **Docuvia2's `doctor` and `uninstall` are fully implemented**, not "Planned" as a prior version of this document claimed. `doctor` ran real checks (SQLite integrity, WAL bloat, git remote reachability, log scan) in 2.88s.
- ✅ **Update (2026-07-22, this pass): the global-config concern below was substantially fixed by IFCE-002** (commit `fd3a71d`, 2026-07-19 — three days before this document's own "2026-07-22" claimed re-verification, meaning the previous revision of this section was already stale when it was written). `init` no longer writes Claude Desktop's global config at all — `claude.platform.ts`'s `installHooks` now only prints a copy-paste-able `mcpServers` snippet (its own doc comment: _"Docuvia never writes machine-global state"_). `uninstall` at the time did a best-effort **read-then-delete** of a legacy global MCP entry, scoped explicitly to cleaning up what a _pre-IFCE-002_ `init` might have written — not a general, ongoing behavior. (That legacy exception has since been removed too — see the resolved item below and action item 4 in §3 — so `uninstall` now never touches the global config file at all.) The original finding (below) is kept for the historical record, since it was a real, correctly-reported observation at the time it was captured; it just describes a since-superseded code path.
  - _Original finding:_ `docuvia uninstall` was executed live during an earlier benchmark and had a real, cross-project side effect: it removed the Docuvia2 MCP server entry from the user's global `claude_desktop_config.json` and deleted the local `.docuvia/local.db`. Flagged because an "uninstall" command for one project silently touching global, shared configuration is surprising behavior for a repo-scoped tool.
  - ~~_Remaining open item:_ `uninstall`'s legacy-cleanup path still writes without a confirmation prompt if it finds a stale entry.~~ **Resolved** (see action item 4 in §3): the legacy read-then-delete path has since been removed entirely — `uninstall` now only prints a reminder, never touching the global config file.
- **`docuvia init` hung waiting on a TTY prompt** when run non-interactively without `--platform`; only proceeds unattended once a platform flag is supplied. This part of the original finding is unchanged and still accurate — see §2b for what actually happens once it proceeds (materially different from what the previous revision of this document claimed).

### 2b. Category: Indexing & Analysis (Graph Building)

Target for all four tools: the Docuvia2 repository itself (420 files, TypeScript/pnpm monorepo).

| Feature / Metric           | Docuvia2                                                       | GitNexus                                                 | Graphify                                           | Code-Review-Graph (CRG)                   |
| :------------------------- | :------------------------------------------------------------- | :------------------------------------------------------- | :------------------------------------------------- | :---------------------------------------- |
| **Full Graph Build**       | `analyze` (full AST ingestion, confirmed)                      | `analyze .`                                              | `extract <path>`                                   | `build --repo .`                          |
| **Verified Build Result**  | **3,970 nodes / 4,816 edges**, 389/389 files parsed (0 failed) | **2,642 nodes / 6,601 edges / 196 clusters / 202 flows** | **1,593 nodes / 4,180 edges** (AST + LLM semantic) | **2,658 nodes / 29,738 edges**, 395 files |
| **Verified Build Latency** | **4.25s** (re-verified 2026-07-22, isolated worktree)          | 27.6s (23.2s internal + npx cold-start)                  | 18.1s (4s AST-only + ~14s LLM semantic)            | 6.03s                                     |
| **Incremental Update**     | _Implicit via hooks (not exercised)_                           | `update`/`detect-changes`                                | `update <path>` (no LLM needed)                    | `update`                                  |
| **Clear Local Index**      | `clean`                                                        | `clean`                                                  | _Manual deletion of `graphify-out/`_               | _N/A_                                     |

**Verified findings (2026-07-22, corrected — see below):**

- ✅ **Correction: Docuvia2's `analyze` does NOT produce zero graph nodes.** The previous two revisions of this document (2026-07-13 and the first pass of 2026-07-22) both claimed `analyze` was tag-detection-only and produced `L2 Nodes: 0`. Re-verified this pass in a disposable git worktree (`pnpm install && pnpm run build`, clean `.docuvia/`): `docuvia analyze` printed `Parsed 389/389 file(s) (0 failed)` and ran real AST ingestion in **4.25s wall-clock**, and the subsequent `docuvia status` reported **L2 Nodes: 3970**. `docuvia snapshot` then confirmed **4,816 edges** alongside those nodes. The primary tree's own real, continuously-dogfooded `.docuvia/local.db` independently corroborates this: it already held 2,852 L2 nodes from ordinary project usage, not 0. The CLI's own `--help` description for `analyze` ("Detect project type/tags from config files") was itself stale and contributed to the earlier misread — corrected in `artifacts/cli/src/constants/cli-commands.ts` in this session to say what the command actually does.
- ✅ **Correction: the "AST worker crash-loop" this document previously reported no longer reproduces.** The exact error quoted in the prior revision (`Cannot find module '.../lib/core/src/constants/encoding.js' imported from .../lib/core/src/ast/ast-worker.ts`) was fixed by commit `5249da9` ("fix(cli,core): fix dist build packaging, AST node_key collisions, and init/status data loss"), dated **2026-07-13 13:48 — 26 minutes after** the commit that first added this benchmark document (`e650143`, 2026-07-13 13:22). In other words: the bug was real when first documented, was fixed the same day, and this document was never updated to reflect that — both the 2026-07-13 revision and the first 2026-07-22 revision kept restating a dead bug. `ast-worker.ts` no longer imports `constants/encoding.js` at all (it inlines the two literals it needs, with a comment explaining exactly why — the same tsx/`worker_threads` resolve-hook limitation the old bug hit). Re-verified this pass via `vitest run src/ast/ast-worker-pool.unit.test.ts` (6/6 passing, including the dev-mode worker-compile path) and via the live `analyze` run above.
- ℹ️ **Related, separately-verified build hygiene note:** the primary tree's checked-in `artifacts/cli/dist/cli.js` was itself stale at the _start_ of this session (last built 2026-07-20, before the unrelated `a489345` fix that stopped `tsc --build` from clobbering `dist/`) and failed with `ERR_MODULE_NOT_FOUND: lib/libgit2/src/register.js` — a different, already-understood, already-fixed issue (see that commit), not the shebang bug below. A fresh `pnpm run build` in the primary tree resolved it. This is a reminder that `dist/` is a build artifact, not a source of truth — always rebuild before benchmarking the compiled path.
- ⚠️ **GitNexus's `analyze` mutated tracked project files as an undocumented side effect.** It appended a `<!-- gitnexus:start -->…<!-- gitnexus:end -->` instructional block to `AGENTS.md` and six files under `.claude/skills/gitnexus/`, and **completely overwrote `CLAUDE.md`**, replacing the entire pre-existing file (the project's real orchestration instructions) with only its own auto-generated block. This was caught and reverted (`CLAUDE.md` restored from source, `AGENTS.md`/skills restored via `git checkout`) during this benchmark. Any team running `gitnexus analyze` on a repo with an existing `CLAUDE.md` should expect data loss unless the file is already committed.
- **Graphify's `extract` failed on the first attempt** — the "gemini" semantic backend requires the `openai` pip package, which is not installed by `pip install -e .` by default, even though `GEMINI_API_KEY` was present in the environment. It only succeeded after manually installing the missing dependency (as the tool's own error message suggested). Once working, it made a real, billed LLM call (~$0.09 via the user's own Gemini key, 138k input / 6.5k output tokens) to produce the semantic layer; the AST-only portion completes in ~4s without any LLM cost.
- **Code-Review-Graph's build is extremely performant on Docuvia2 (6.03s)**, parsing 399 files and generating a robust graph of 2,658 nodes and 29,738 edges.

**Note on this session's worktree incident (2026-07-22):** re-verifying the numbers above involved running `docuvia snapshot` inside the disposable Docuvia2 self-worktree (`../docuvia2-benchmark`) to confirm the AST fix flowed all the way through to the git-native sync layer. That command packed the worktree's _test_ graph onto `docuvia-knowledge` — but because a git worktree shares its `.git` ref store with the primary tree, this silently moved the _primary tree's real_ `docuvia-knowledge` branch tip from its genuine last snapshot (`d62d0c1`, tied to real commit `5e7ec34`) to the disposable test snapshot (`abb4bd4`). This was caught immediately via `git reflog show docuvia-knowledge` and reverted (`git update-ref refs/heads/docuvia-knowledge d62d0c1`) before the worktree was removed — no data was lost, but it is exactly the class of incident §0's isolation rule exists to prevent, and that rule has been updated (see §0) to call out that snapshot/hydrate/sync-knowledge specifically need extra care even inside a self-worktree, unlike the working-directory-only commands (`analyze`/`query`/`impact`/`export-topology`).

### 2c. Category: Query, Visualization & Impact

All commands below were run against the graph each tool built in §2b for Docuvia2 (except where noted).

| Feature / Metric          | Docuvia2                                                                                                                                 | GitNexus                                                                                                                                   | Graphify                                                                    | Code-Review-Graph (CRG)                                                                    |
| :------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------- |
| **Query Engine**          | `query "<symbol>"` — **0.54s, real result** (e.g. `query "AstWorkerPool"` resolved the module and listed its real incoming caller)       | `query` — 248ms internal / 4.35s incl. npx cold-start, real ranked results with per-stage timing (vector/bm25/merge/symbol_lookup/ranking) | `query "<question>"` — 0.43s, real BFS results (token-budgeted)             | `query` (e.g. `query callers_of <target>`) — 0.69s, returns structured JSON                |
| **Impact / Blast Radius** | `impact "<symbol>"` — **1.26s, real result** (e.g. `impact "AstWorkerPool"` returned the real dependent module and `Risk level: MEDIUM`) | `impact` — real result: exact symbol resolved, `impactedCount: 0`, `risk: LOW`, `epistemic: "exact"`                                       | `affected "X"` — 0.36s, real reverse-traversal results                      | `detect-changes` — 0.46s, real per-symbol `risk_score` + estimated token savings panel     |
| **Explain / Context**     | _N/A_                                                                                                                                    | `context`, `trace`, `cypher` (not exercised this run)                                                                                      | `explain "X"` — 0.44s, real connection list with edge types and node degree | Supported via rich CLI subcommands (`query`, `flows`, `communities`, `architecture`, etc.) |
| **Visual Export**         | `export-topology` — **0.43s, real result**: `389 nodes, 52 links, 11 groups` written to `topology.json`/`.html`                          | _N/A (web UI only)_                                                                                                                        | `graph.html` generated as part of `extract`/`cluster-only`                  | `visualize` — 0.98s, real 3.8MB interactive `graph.html`                                   |
| **Docs / Wiki Gen**       | _N/A_                                                                                                                                    | `wiki` (not exercised this run)                                                                                                            | `label`/`cluster-only` (community naming, requires LLM)                     | `wiki` — 0.48s, generated 11 markdown wiki pages                                           |

**Verified findings (2026-07-22, corrected):**

- ✅ **Correction: Docuvia2's `impact`/`query`/`export-topology` are no longer empty-result no-ops.** The previous two revisions of this document reported all three as fast-but-useless because §2b's graph was believed to be empty. Now that §2b's correction stands (the graph has 3,970 real nodes), all three were re-run against it in the same isolated worktree and returned genuine, correct, sub-second results — see the table above. This was a direct downstream consequence of the AST worker fix, not a separate change to these commands.
- **GitNexus's `impact`/`query` are the only ones in this benchmark that surfaced a queryable multi-repo ambiguity** — because GitNexus globally registers every repo it indexes on the machine, `query`/`impact` failed with "Multiple repositories indexed" until `--repo <path>` was supplied explicitly. This is a real UX papercut for a single-repo workflow, not a broken feature.
- **Graphify and Code-Review-Graph both returned real, correct, sub-second results** on every read-side command tested. CRG has unique risk-scored `detect-changes` with a context-savings estimate, as well as a full suite of terminal queries.

### 2d. Category: Remote Sync & Git Integration

| Feature / Metric         | Docuvia2                                                                                                                                                                                                                                                                                                                      | GitNexus                                                               | Graphify                                         | Code-Review-Graph (CRG) |
| :----------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------- | :----------------------------------------------- | :---------------------- |
| **Push Analysis to API** | `sync` — attempted live; failed non-interactively with "Project ID is required when not running interactively" (requires remote auth/config not available in this environment)                                                                                                                                                | `publish` (opt-in, requires `UNDERSTAND_QUICKLY_TOKEN`; not exercised) | _N/A_                                            | _N/A_                   |
| **Commit Graph to Git**  | `snapshot` — **verified working with real data**: packed **3,970 nodes / 4,816 edges** onto the `docuvia-knowledge` branch in 1.93s (see the worktree-incident note above for a caveat on where to run this safely)                                                                                                           | _N/A_                                                                  | `merge-driver` (JSON union-merge, not exercised) | _N/A_                   |
| **Hydrate from Git**     | `hydrate` — **verified working with real data**: after `docuvia clean` wiped the local DB, the very next `docuvia status` auto-rehydrated **3,970 nodes / 4,816 edges / 0 dropped** straight from the `docuvia-knowledge` branch — a genuine, lossless round-trip, not the empty 0-node reload the previous revision reported | _N/A_                                                                  | _N/A_                                            | _N/A_                   |
| **Cross-Clone Sync**     | `sync-knowledge` (not exercised this run — requires a second clone to be meaningful)                                                                                                                                                                                                                                          | _N/A_                                                                  | _N/A_                                            | _N/A_                   |

**Verified findings (2026-07-22, corrected):**

- ✅ **Correction: Docuvia2's git-native snapshot/hydrate mechanism no longer ships an empty payload.** The previous revision's core caveat — "the pipe works, there is nothing flowing through it yet" — is resolved now that §2b's AST fix means `analyze` populates a real graph. This remains the only tool of the four in this benchmark that commits graph state to a hidden git branch (rather than a local-only cache), and it is now verified end-to-end with real data: build → snapshot → clean → auto-rehydrate, all lossless.

---

## 3. Architecture & Product Recommendations (Re-evaluated: 2026-07-22, based on live-verified results)

### System Architect's Evaluation

> **Score (Max 100) — revised 2026-07-22 (this pass) after both of Docuvia2's previously-reported blocking bugs failed to reproduce against current source and were re-verified as fixed:**
>
> 1. **Docuvia2: 87 / 100** (Up from the previous revision's 52 — that score was computed against two bugs that had already been fixed nine days earlier; see the correction note below. Re-verified fresh in this pass: fastest full build of the four tools at **4.25s**, and the highest node count at **3,970 nodes / 4,816 edges** on the same target repo. `query`/`impact`/`export-topology` all return real, correct, sub-second results. Uniquely, it is the only tool of the four with a working git-native snapshot/hydrate round-trip — graph state commits to a hidden branch instead of a local-only cache, verified lossless in this pass (build → snapshot → clean → auto-rehydrate). Cons _(at the time of this 2026-07-22 score — the `init` cons is stale, see action item 3's correction: fixed by IFCE-004, 2026-07-27)_: `init` still hangs on a bare TTY prompt without `--platform` and has no CI/non-interactive flag; no wiki-gen or rich exploration subcommands (`flows`/`communities`/`architecture`) the way CRG has. (`uninstall`'s legacy global-config touch, previously flagged in action item 4, has since been removed entirely — see the note in §3.))
> 2. **Code-Review-Graph (CRG): 88 / 100** (Uniquely powerful and complete CLI query/exploration toolset including `query`, `detect-changes`, `flows`, `communities`, and `architecture`. Full build on Docuvia2 (399 files) is incredibly fast at 6.03s, and incremental updates are sub-second. Its `detect-changes` provides a highly valuable token-saving panel showing exact optimization percentages. Cons: Community detection degrades to file-based groups if the optional `igraph` is not installed, and it lacks Git-native sync capabilities (local-first design focus).)
> 3. **GitNexus: 82 / 100** (Only tool whose query/impact/analyze all worked end-to-end on real data with correct results. Cons: ~2,642-node build took 27.6s including cold-start, multi-repo query ambiguity needs manual `--repo`, and **`analyze` overwrote the target repo's `CLAUDE.md` and mutated `AGENTS.md`/skill files without confirmation** — a real safety issue for a "read-only" indexing tool.)
> 4. **Graphify: 80 / 100** (Fastest read-side commands (all sub-second), richest editor-hook breadth, real semantic explain via LLM. Cons: full `extract` failed out of the box on a missing optional dependency despite an API key being present, and the semantic layer has a real per-run dollar cost.)
>
> CRG and Docuvia2 are close enough (88 vs. 87) that the ranking could reasonably flip depending on whether raw exploration breadth (CRG's `flows`/`communities`/`architecture`/`wiki`) or git-native, versioned knowledge (Docuvia2's unique differentiator) matters more for a given team's workflow.

### Correction: the two "Verified, Reproducible Bugs" this document previously reported are fixed

The previous two revisions of this document (2026-07-13, and the first pass of 2026-07-22) listed the following as live, reproducible bugs. Both were re-tested this pass (fresh `pnpm run build`, live command execution) and **neither reproduces against current source.** Kept here for the historical record, with root-cause commits:

1. ~~`artifacts/cli/dist/cli.js` cannot execute — duplicated shebang from `tsup`'s `banner.js`.~~ **Fixed by commit `5249da9`, 2026-07-13** (the same day this document first reported it). Re-verified this pass: a fresh `node dist/cli.js` (rebuilt in both the primary tree and an isolated worktree) runs correctly with a single shebang line. Separately, the primary tree's checked-in `dist/` had gone stale for an unrelated reason (see §2b's build-hygiene note) — rebuilding fixed that too.
2. ~~`docuvia init`/AST worker pool crash-loops on `Cannot find module '.../constants/encoding.js'`.~~ **Fixed by commit `5249da9`, 2026-07-13** (same commit, same day). Re-verified this pass via `ast-worker-pool.unit.test.ts` (6/6 passing) and a live `analyze` run producing 3,970 real nodes — see §2b.

**Why this document kept reporting them as current for over a week:** the fix commit landed 26 minutes after the document's first revision, but nothing re-ran the benchmark against post-fix source until this pass — the second 2026-07-22 revision changed some CRG numbers and dates but did not actually re-execute the Docuvia2 commands, so it silently carried the dead bugs forward. Lesson for future re-runs: §1 rule 3's "MUST NOT hallucinate" applies just as much to _reusing a stale prior finding_ as it does to fabricating a new one — treat every number in this document as needing fresh verification, not just the ones that look like they might have changed.

### Strategic Action Items for Docuvia2 (revised priority order, based on this pass's verified evidence)

1. ~~Fix the duplicated shebang in the `tsup` build.~~ **Done** (`5249da9`, 2026-07-13).
2. ~~Fix the AST worker's module resolution crash.~~ **Done** (`5249da9`, 2026-07-13).

3. ~~Add a non-interactive/CI mode to `init`.~~ **Done — [IFCE-004](../adr/interface/IFCE-004-explicit-interactive-opt-in.md), accepted 2026-07-27.** Not via the `--yes` flag proposed here; instead every interactive prompt in the CLI (including `init`'s platform checkbox) became opt-in via `--interactive`/`-i`, so `init` with no flags now proceeds straight to its non-interactive default (every platform) and never blocks on a TTY prompt, regardless of what `stdin.isTTY` reports.

4. ~~Treat `uninstall`'s legacy global-config cleanup as an explicit, confirmed action.~~ **Done — removed entirely, stronger than the originally-proposed fix** (`claude.platform.ts`, `printMcpServerRemovalReminder`).
   - IFCE-002 (commit `fd3a71d`, 2026-07-19) already stopped `init` from ever writing global state. `uninstall`'s remaining global-config touch (a legacy best-effort removal of a pre-IFCE-002 entry) has since been removed too, rather than just gated behind a confirmation prompt as originally proposed here: `uninstallHooks` now only calls `printMcpServerRemovalReminder`, which prints the resolved global config path and tells the user to remove the entry manually — zero fs reads/writes against that file, matching `init`'s IFCE-002-compliant print-only behavior.

5. ~~Borrow visualization ideas from Graphify/CRG.~~ **Correction (2026-08-02): this item's premise was wrong — `export-topology` already produced a real interactive HTML file (canvas force-layout, pan/zoom, search, group hulls, click-to-inspect blast-radius highlighting — `topology-html-template.ts`) since commit `624fad5a`, 2026-07-12, ten days before this document's 2026-07-22 pass claimed "no equivalently rich rendering."** That pass verified the node/link/group counts and `.json` output but apparently never opened the `.html` file it also wrote. Separately, [`roadmap-and-open-items.md`](roadmap-and-open-items.md) item 5 shipped 2026-07-28, enriching the _data_ fed into that pre-existing template (L2 type, decision confidence/validity, edge commit-sha/diff-summary) — a real, smaller improvement, not the "no visualization exists" gap this item originally described.

### Note on GitNexus (separate from Docuvia2, flagged for the maintainers of that project)

`gitnexus analyze` overwriting a target repository's `CLAUDE.md` and silently appending to `AGENTS.md`/skill files is a real finding from this run, not specific to Docuvia2 — any repo with an existing `CLAUDE.md` that runs `gitnexus analyze` should expect it to be replaced. This should be raised with the GitNexus maintainers; a safe fix would be to only ever append/merge between `<!-- gitnexus:start -->`/`<!-- gitnexus:end -->` markers and never touch content outside them, the way it already (correctly) does for `AGENTS.md`.

> **Follow-up (2026-08-02):** see [`gitnexus-claude-md-overwrite-investigation-2026-08-02.md`](gitnexus-claude-md-overwrite-investigation-2026-08-02.md) — reading GitNexus's current source shows `CLAUDE.md` and `AGENTS.md` are already written by the identical marker-bounded upsert function, a design in place since 2026-02-04 (`747cf003`), predating this benchmark run by five months. The overwrite described above could not be reproduced from source inspection alone; that document lays out why and what to check before filing anything upstream.
