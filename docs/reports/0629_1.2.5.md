# Design Verification Report — Item 1.2.5

**Item ID:** 1.2.5
**Description:** SVN integration — architecture drift: diffs stored improperly
**Verification Date:** 2026-06-26
**Verdict:** ⚠️ WARN
**Type:** Re-verification (previous: 0378_1.2.5.md, 2026-07-27 — REPORT FILE LOST)

---

## Design Spec References

| Document | Section | Description |
|----------|---------|-------------|
| `master-roadmap.md` | Phase 2 (Input Layer) | SVN as ingestion source alongside Git |
| `docs/design/adrs/ADR-004-git-isomorphic-graph.md` | Full | Git-isomorphic knowledge graph — SVN should mirror this pattern |
| `docs/design/adrs/ADR-014-sql-indexed-graph-and-database-as-ipc.md` | Full | Database-as-IPC principle — diff data should be structurally indexed, not blob-concatenated |

---

## Source Files Examined

| File | Purpose |
|------|---------|
| `artifacts/api-server/src/lib/svn-client.ts` | SVN CLI wrapper: log streaming + diff fetching |
| `artifacts/api-server/src/routes/ingest.ts` | SVN ingestion route handler |
| `artifacts/api-server/src/lib/ingestion-pipeline.ts` | Shared ingestion pipeline (Git + SVN + Document) |
| `lib/db/src/schema/commits.ts` | Commits table schema |

**Checksums (SHA-256):**

| File | Hash |
|------|------|
| `artifacts/api-server/src/lib/svn-client.ts` | `4f1333992e504fd7c2216bb0c32f9f1372ca159420108241ab8b52b4b777f580` |
| `artifacts/api-server/src/routes/ingest.ts` | `c51b5ff07b286697e700197802f022a8fb35be06723064be261c71aaaaa9173b` |
| `artifacts/api-server/src/lib/ingestion-pipeline.ts` | `b7784548df3735fabda19abb186897fe788f5d8e5cf014f0b308c63ea1f6b8d7` |
| `lib/db/src/schema/commits.ts` | `c5d864a71bc89785f77a63e9985189bd3c8bb48f3e8c42423e84642927907ff8` |

---

## Round 1 — Architecture & Design Review

### Design ↔ Implementation Alignment

**✅ Correctly implemented:**

1. **Streaming log parser** — `getSvnLog()` uses `AsyncGenerator` with SAX streaming parsing of `svn log --xml`. Memory-efficient, backpressure-aware, properly cleans up subprocess via `AbortController` in `finally` block.

2. **Shell injection prevention** — All subprocess invocations use `spawn()`/`execFile()` with argument arrays. URL validation enforces `https?://`, `svn://`, or `svn+ssh://` scheme.

3. **Incremental ingestion** — `lastSvnRevision` on `projectsTable` correctly tracks last ingested revision. Route uses `mode: "incremental"` to start from `lastSvnRevision + 1`.

4. **Revision-based deduplication** — `processIngestion()` checks for existing commits with `(revision, vcsType: "svn", projectId)` predicate.

5. **Batching** — Ingestion route buffers up to 50 items before flushing via `processIngestion()`.

6. **Error resilience** — Individual diff fetch failures are caught and logged (`svnErrors`) without aborting the entire ingestion.
7. **Auth middleware** — `requireApiKey` is applied to the SVN route (added since 2026-07-27 verification). This fixes the prior auth finding.

### Gaps / Deviations

1. **⚠️ Diff concatenation into message field (persistent drift)** — `ingestion-pipeline.ts:153`: `fullMessage = c.diff ? \`${c.message}\n\n${c.diff}\` : c.message`, stored into `commitsTable.message` (truncated to 4000 chars). This remains unchanged from the 2026-07-27 verification. Consequences:
   - Diffs exceeding `4000 - message.length` chars are silently truncated
   - Diff content is not separately queryable for graph/L3 generation
   - Git commits (line 99) store `c.message.slice(0, 4000)` without diff concatenation — SVN is inconsistent

2. **⚠️ No `diff` column in schema** — `commitsTable` has no dedicated diff storage column. Both Git and SVN lose structural diff data. This limits L3 node generation quality for SVN sources.

3. **⚠️ Synthetic commit hash** — SVN uses `hash: \`svn:R${c.revision}\`` which provides no content integrity verification (unlike Git SHA hashes).

---

## Round 2 — Code Quality & Security Review

### Strengths

1. **Command injection protection** — `spawn()` with array args, `execFile()` with array args, regex-validated URL. No shell interpolation risk. **Unchanged since 2026-07-27 — still solid.**

2. **Resource cleanup** — `getSvnLog()` uses `AbortController` + `finally { abortController.abort(); svnProcess.kill(); }`. Prevents zombie SVN processes.

3. **Authentication** — `--no-auth-cache` and `--non-interactive` flags prevent credential persistence. `requireApiKey` applied to route.

4. **ScoreCommit refactor** — Scoring API updated from `score >= 0.4` threshold to `{ valid }` return — cleaner semantics.

### Issues Found

1. **⚠️ SVN Git pipeline divergence (transaction batching)** — Git path uses batched DB transactions (`db.transaction()` with `batchSize = 50`), while SVN processes items one-by-one in a simple `for` loop. For large SVN histories, this is N individual INSERTs without transaction grouping.

2. **� SAX parser error state** — The SAX parser's `error` event calls `reportError()` but does not reset the parser. After a malformed XML entry, subsequent entries may be silently lost.

3. **� No rate limiting on diff fetching** — `getSvnDiff()` is called sequentially per revision. Large SVN histories can create sustained load against the SVN server.

---

## Round 3 — Integration & Completeness Review

### Integration Correctness

1. **SVN log → ingestion pipeline → commit records** flows end-to-end. Incremental cursor (`lastSvnRevision`) updated correctly.

2. **Commit scoring** uses `scoreCommit(c.message, c.diff)` consistently for both Git and SVN paths.

3. **Activity log and notification** — `logAndNotify()` called for SVN ingested commits.

### Missing Coverage

1. **No dedicated SVN test coverage** — Unit tests for `getSvnLog()` and `getSvnDiff()` require SVN server mock infrastructure.

2. **Diff truncation not handled gracefully** — When diff exceeds 4000-char budget, content is lost without warning/indicator.

---

## Changes Since Last Verification

| Change | Impact |
|--------|--------|
| `svn-client.ts` | **No change** (identical checksum) |
| `ingest.ts` | **Modified** — added `requireApiKey` (auth finding FIXED) |
| `ingestion-pipeline.ts` | **Modified** — refactored `scoreCommit` API (no behavior change for diff storage issue) |
| `commits.ts` | **No change** (identical checksum) |

**Net change:** Auth finding from 2026-07-27 is resolved. All other findings (diff concatenation, synthetic hashes, transaction divergence) are carried forward. The core architecture drift finding persists.

**Note:** The original report `0378_1.2.5.md` (2026-07-27) has been lost from disk — it was committed to `docs/roadmap/reports/` (old path) which no longer exists. Findings from that report have been verified as still applicable (except auth).

---

## Findings Summary

| # | Severity | Category | Finding | Status |
|---|----------|----------|---------|--------|
| 1 | � | Architecture | Diff concatenated into `message` field (4000-char truncation) — violates ADR-014 structural indexing principle | Open (unchanged) |
| 2 | � | Architecture | No `diff` column in `commitsTable` — SVN and Git both lose diff context > 4KB | Open (unchanged) |
| 3 | 🟢 | Security | No auth on SVN ingestion route | **RESOLVED** (requireApiKey added) |
| 4 | 🟢 | Architecture | Synthetic `svn:R${revision}` hash provides no content integrity | Open (unchanged) |
| 5 | � | Performance | SVN uses per-row INSERT without transaction batching (Git uses batched tx) | Open (unchanged) |
| 6 | 🟢 | Resilience | SAX parser error state not reset; no rate limiting on diff fetch | Open (unchanged) |

---

## Overall Verdict

**⚠️ WARN**

The SVN integration is functional for basic use cases — log streaming, auth, incremental mode, deduplication, and error handling all work correctly. The 2026-07-27 auth finding has been **resolved** by adding `requireApiKey`. However, the core architecture drift (diff blob-concatenation into message field) persists unchanged. This is not a breaking defect (SVN ingestion works end-to-end), but it prevents proper L3 node generation from SVN diff content and creates inconsistency with ADR-014's structural indexing principle. Resolution requires either: (a) adding a `diff` column to `commitsTable` and storing diffs separately, or (b) documenting the concatenation as intentional and the 4000-char truncation as an explicit design constraint.
