# Verification Report: 1.1.4 — Test factories for DB state creation (factories.ts)

**Item ID:** 1.1.4
**Description:** Test factories for DB state creation (factories.ts)
**Date:** 2026-06-12
**Verdict:** ⚠️ WARN

## Team Review (Architecture & SRE)
- **Leo (Architect)**: Testing infrastructure is brittle. The factory pattern needs to be expanded for all tables using `Partial<InsertModel>` and `withRollback` transactions to ensure clean test state without foreign key violations.
- **Max (SRE / Security)**: Test isolation is critical. Ensure `faker` randomizers are seeded to prevent flaky, non-deterministic test failures that plague CI pipelines.
- **Conclusion**: Needs immediate remediation. Build out `CommitFactory`, `DocumentFactory`, `L1TagFactory`, `ReviewTaskFactory`, etc., in `factories.ts`.

## Target Files
The following files are targeted for this implementation/fix:
- [`artifacts/api-server/test/support/factories.ts`](file:///d:/GitHub/Docuvia/artifacts/api-server/test/support/factories.ts)
- [`lib/db/src/schema/activity_log.ts`](file:///d:/GitHub/Docuvia/lib/db/src/schema/activity_log.ts)
