# Verification Report: 1.3.5 — Hybrid search: vector + graph merge and re-rank

**Item ID:** 1.3.5
**Description:** Hybrid search: vector + graph merge and re-rank
**Date:** 2026-06-12
**Verdict:** ⚠️ WARN

## Team Review (Architecture & SRE)
- **Leo (Architect)**: The backend hybrid logic is decent, but the frontend (`query.tsx`) is still hitting the legacy `/search` endpoint instead of the new `/mcp/query` endpoint. We also need to boost scores for nodes found by both signals.
- **Max (SRE / Security)**: If hybrid search calls multiple async paths, use `Promise.allSettled`. We cannot afford the entire pipeline failing just because the graph traversal timed out while the vector search succeeded.
- **Conclusion**: Needs immediate remediation (UI integration & Fault-tolerance).

## Target Files
The following files are targeted for this implementation/fix:
- [`artifacts/api-server/src/lib/intent-router.ts`](file:///d:/GitHub/Docuvia/artifacts/api-server/src/lib/intent-router.ts)
- [`artifacts/kg-engine/src/pages/QueryPage.tsx`](file:///d:/GitHub/Docuvia/artifacts/kg-engine/src/pages/QueryPage.tsx)
