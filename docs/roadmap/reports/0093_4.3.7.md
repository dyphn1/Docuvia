# Verification Report: 4.3.7 — Schema: branchName column on commits

**Item ID:** 4.3.7
**Description:** Schema: branchName column on commits
**Date: 2026-06-12**
**Verdict:** ✅ PASS

## Implementation Details
- The column ranchName is defined on commitsTable in [commits.ts](file:///d:/GitHub/Docuvia/lib/db/src/schema/commits.ts#L22):
  \	ypescript
  branchName: text("branch_name"),
  \\n- In the git synchronization route [sync.ts](file:///d:/GitHub/Docuvia/artifacts/api-server/src/routes/sync.ts#L121), the column is updated with the branch name when commits are pushed:
  \	ypescript
  if (pushedCommits.length > 0) {
    await db
      .update(commitsTable)
      .set({ branchName: pushedBranch })
      .where(
        and(
          eq(commitsTable.projectId, projectId),
          inArray(commitsTable.hash, pushedCommits)
        )
      );
  }
  \\n