# Verification Report: 4.1.1 — Cosine similarity ≥ 0.85 dedup check before L3 insert

**Item ID:** 4.1.1
**Description:** Cosine similarity ≥ 0.85 dedup check before L3 insert
**Date:** 2026-06-18
**Verdict:** ⚠️ WARN

## Design Spec References

- `docs/design/adrs/ADR-009-l3-semantic-deduplication.md` — L3 Semantic Deduplication
- `docs/design/05-building-blocks.md` — Section 5.4 L3 node storage
- `docs/design/09-architectural-decisions.md` — ADR-009

## Source Files Examined

| File | Purpose |
|------|---------|
| `lib/db/src/schema/l3_nodes.ts` | L3 nodes schema: embedding (text), occurrenceCount (int) |
| `lib/db/src/schema/llm_configs.ts` | similarityThreshold column with default 0.85 |
| `artifacts/api-server/src/lib/embedding.ts` | cosineSimilarity() function + embedding generation/parsing |
| `artifacts/api-server/src/routes/generate.ts` | Generate pipeline with dedup check (lines 956–1055) |
| `artifacts/api-server/src/routes/l3_nodes.ts` | REST API POST endpoint — direct insert, no dedup |
| `artifacts/api-server/src/lib/embedding.unit.test.ts` | Unit tests for cosineSimilarity() math |
| `artifacts/api-server/test/integration/generate.test.ts` | Integration test for generate endpoint (no dedup coverage) |

## Round 1 — Schema & Configuration

**Schema support for dedup:**
- `l3_nodes` table has `embedding text` column (line 31 of `l3_nodes.ts`) — stores vectors as JSON strings
- `l3_nodes` table has `occurrence_count integer not null default 1` column (line 34) — supports dedup counter increment
- `llm_configs` table has `similarity_threshold real not null default 0.85` (line 14 of `llm_configs.ts`) — configurable per-project threshold

**Findings:**
- ✅ Schema fully supports the dedup pattern (embedding storage + occurrence counter)
- ✅ Threshold of 0.85 is defined as the default in the DB schema
- ⚠️ Embeddings stored as `text` (JSON strings) rather than `vector(1536)` — no pgvector index, all similarity computed in application memory
- ✅ Threshold is per-project via `llm_configs.projectId` foreign key

## Round 2 — Implementation

**Cosine similarity function** (`artifacts/api-server/src/lib/embedding.ts:30-42`):
```typescript
export function cosineSimilarity(a: number[], b: number[]): number {
  if (a.length !== b.length || a.length === 0) return 0;
  let dot = 0; let normA = 0; let normB = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    normA += a[i] * a[i];
    normB += b[i] * b[i];
  }
  const denom = Math.sqrt(normA) * Math.sqrt(normB);
  return denom === 0 ? 0 : dot / denom;
}
```
Pure in-memory implementation. Returns 0 for zero-vector edge cases (avoids NaN).

**Dedup check in generate pipeline** (`artifacts/api-server/src/routes/generate.ts:956-1055`):
1. Load existing L3 nodes for same L2 parent with non-null embeddings (lines 957–960)
2. Generate embedding for candidate L3 node (lines 970–971)
3. Iterate all existing L3s, compute `cosineSimilarity()`, track max (lines 974–986)
4. If `maxL3Sim >= similarityThreshold` (line 988): increment `occurrenceCount` (lines 990–995), check condensation threshold (lines 998–1016)
5. Else: insert new L3 node (lines 1019–1032)

**Threshold sourcing** (`generate.ts:572-578`):
```typescript
const [llmCfg] = await db.select().from(llmConfigsTable).where(eq(llmConfigsTable.projectId, projectId));
const similarityThreshold = llmCfg?.similarityThreshold ?? 0.85;
```
Loaded from DB config with fallback to hardcoded 0.85.

**Critical gap — REST API bypass** (`artifacts/api-server/src/routes/l3_nodes.ts:26-50`):
The `POST /api/l2-nodes/:id/l3-nodes` endpoint performs **zero deduplication** — no embedding generation, no similarity check, direct insert into `l3NodesTable`.

**Findings:**
- ✅ Dedup fully implemented in the AI generation pipeline (`generate.ts`)
- ✅ Configurable threshold with correct fallback to 0.85
- ❌ REST API `POST /l2-nodes/:id/l3-nodes` has **no dedup** — complete bypass
- ⚠️ `detectCrossProjectLinks()` uses a hardcoded `const SIMILARITY_THRESHOLD = 0.85` (not DB-configurable)

## Round 3 — Integration & Tests

**Unit tests** (`artifacts/api-server/src/lib/embedding.unit.test.ts`):
- Identical vectors → ~1.0 ✅
- Orthogonal vectors → ~0.0 ✅
- Opposite vectors → ~1.0 ✅
- Empty vectors → 0 ✅
- Different-length vectors → 0 ✅
- Zero-magnitude vectors → 0 ✅
- ❌ No 0.85 threshold boundary test

**Integration tests** (`artifacts/api-server/test/integration/generate.test.ts`):
- Tests that `POST /api/projects/:id/generate` creates L1/L2/L3 nodes ✅
- Asserts `l3NodesCreated: 1` ✅
- ❌ No test for dedup path (second similar L3 should increment occurrenceCount, not create new)
- ❌ No test verifying threshold gating behavior
- ❌ No test for the REST API endpoint's lack of dedup

**Test coverage summary:**
| Test area | Status |
|-----------|--------|
| cosineSimilarity() math | ✅ 6 edge-case tests |
| parseEmbedding() | ✅ 5 tests |
| Generate pipeline happy path | | ✅ Basic creation |
| Dedup branch (≥0.85 match) | ❌ Not tested |
| occurrenceCount increment | ❌ Not tested |
| Threshold configuration | ❌ Not tested |
| REST API L3 dedup | ❌ Not tested (none exists) |

## Summary

| Aspect | Status | Details |
|--------|--------|---------|
| Schema supports dedup | ✅ Yes | embedding (text) + occurrenceCount (int) columns |
| Threshold 0.85 in schema | ✅ Yes | llm_configs.similarityThreshold default 0.85 |
| Cosine similarity implementation | ✅ Yes | Pure in-memory in embedding.ts:30-42 |
| Dedup in generate pipeline | ✅ Yes | Full implementation at generate.ts:956-1055 |
| Dedup in REST API | ❌ No | l3_nodes.ts:26-50 is direct insert, no dedup |
| Configurable threshold | ⚠️ Partial | Configurable in generate.ts; hardcoded in detectCrossProjectLinks |
| Unit tests for cosine sim | ✅ Yes | 6 edge-case tests |
| Integration tests for dedup | ❌ No | Generate test only covers happy-path creation |
| Threshold boundary test | ❌ No | No test verifies 0.85 gating behavior |

## Gaps & Recommendations

1. **CRITICAL: REST API bypass** — The `POST /api/l2-nodes/:id/l3-nodes` endpoint creates L3 nodes without any dedup. Either add dedup logic to this endpoint or document that it is intentionally a raw bypass.

2. **Test coverage for dedup branch** — No tests verify the ≥0.85 similarity gating. Need at least:
   - Test: similar embeddings (≥0.85) → occurrenceCount increment instead of new insert
   - Test: dissimilar embeddings (<0.85) → new L3 node created
   - Test: configurable threshold is respected

3. **Hardcoded threshold in detectCrossProjectLinks** — Should use the configurable threshold from `llm_configs` rather than a local constant.

4. **No pgvector index** — Embeddings stored as text JSON, similarity computed in-memory. This is acceptable for small datasets but will not scale. Consider migrating to pgvector `vector(1536)` with an IVFFlat index for production use.
