# Verification Report: 4.1.2 — occurrenceCount increment on match

**Item ID:** 4.1.2
**Description:** occurrenceCount increment on match
**Date:** 2026-06-18
**Verdict:** ⚠️ WARN

## Design Spec References

- `docs/design/09-architectural-decisions.md` — ADR-009 (lines 221–229): dedup increments `occurrenceCount` on cosine ≥ 0.85 match
- `docs/design/12-glossary.md` — Occurrence Count definition: counter increments each time a new commit's content is found semantically similar (cosine ≥ 0.85) to an existing node
- `docs/ai_plans/implement_v2-core-architecture.md` — Increment `occurrenceCount` on matching node (line 113)

## Source Files Examined

| File                                                     | Purpose                                                                        |
| -------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `lib/db/src/schema/l3_nodes.ts`                          | L3 nodes schema: `occurrenceCount` column definition                           |
| `lib/db/drizzle/0000_short_ezekiel.sql`                  | Initial migration SQL with `occurrence_count` column                           |
| `artifacts/api-server/src/routes/generate.ts`            | Generate pipeline: dedup logic and occurrenceCount increment (lines 988–1016)  |
| `artifacts/api-server/src/lib/orphan-branch-writer.ts`   | Orphan branch writer: serializes `occurrenceCount` to markdown frontmatter     |
| `artifacts/api-server/src/lib/intent-router.ts`          | Graph search: orders L3 results by `occurrenceCount DESC` (line 457)           |
| `artifacts/api-server/src/routes/l3_nodes.ts`            | REST API POST endpoint — direct insert, no dedup or increment                  |
| `artifacts/api-server/test/integration/generate.test.ts` | Integration test for generate endpoint (no occurrenceCount increment coverage) |

## Round 1 — Implementation Analysis

**Core increment logic** (`artifacts/api-server/src/routes/generate.ts:988–995`):

```typescript
if (maxL3Sim >= similarityThreshold && bestL3Match) {
  // Dedup: increment occurrence count
  const newOccurrenceCount = bestL3Match.occurrenceCount + 1;

  await db
    .update(l3NodesTable)
    .set({ occurrenceCount: newOccurrenceCount })
    .where(eq(l3NodesTable.id, bestL3Match.id));
  // ... condensation check follows
}
```

The increment is a simple read-then-write: `bestL3Match.occurrenceCount + 1`, persisted via a Drizzle `update().set().where()` chain targeting the matched L3 node's `id`.

**New node creation** (`generate.ts:1029`):

```typescript
occurrenceCount: 1,
```

New L3 nodes are inserted with `occurrenceCount: 1` (not 0), meaning the counter starts at first occurrence.

**Orphan branch serialization** (`orphan-branch-writer.ts:62`):

```typescript
`occurrence_count: ${node.occurrenceCount}`,
```

The counter is correctly serialized to the orphan branch markdown frontmatter, ensuring it survives git-isomorphic sync.

**Intent-router ordering** (`intent-router.ts:457`):

```typescript
.orderBy(sql`${l3NodesTable.occurrenceCount} DESC`)
```

L3 results in graph search are sorted by occurrenceCount descending — high-frequency decisions surface first.

**Findings:**

- ✅ Increment logic is correct: reads current value, adds 1, writes back
- ✅ New nodes start at `occurrenceCount: 1`
- ✅ Counter is serialized to orphan branch for git-isomorphic sync
- ✅ Counter is used as a quality signal in intent-router ordering
- ⚠️ **No atomic increment** — the read-then-write is not wrapped in a transaction or advisory lock; concurrent generate requests could race and lose an increment
- ❌ REST API `POST /api/l2-nodes/:id/l3-nodes` creates L3 nodes with `occurrenceCount: 1` but never increments on match (no dedup at all — same gap as 4.1.1)

## Round 2 — Schema & Migration Verification

**Drizzle schema** (`lib/db/src/schema/l3_nodes.ts:34`):

```typescript
occurrenceCount: integer("occurrence_count").notNull().default(1),
```

**Migration SQL** (`lib/db/drizzle/0000_short_ezekiel.sql:84`):

```sql
"occurrence_count" integer DEFAULT 1 NOT NULL,
```

**Drizzle snapshot** (`lib/db/drizzle/meta/0000_snapshot.json`):
The snapshot includes `occurrence_count` with the correct column definition.

**Findings:**

- ✅ Schema defines `occurrenceCount` as `integer NOT NULL DEFAULT 1`
- ✅ Migration SQL matches the schema definition
- ✅ Drizzle snapshot is consistent with both
- ✅ `insertL3NodeSchema` (via `createInsertSchema`) includes `occurrenceCount` in the insert schema, allowing it to be set explicitly or default to 1
- ✅ The `updateL3NodeSchema` (partial of insert schema) allows `occurrenceCount` to be updated — the increment path uses this correctly

## Round 3 — Integration & Tests

**Test search results:**

- ❌ No unit test for the occurrenceCount increment logic
- ❌ No integration test verifying that a second similar L3 candidate increments `occurrenceCount` instead of creating a new row
- ❌ No test for the condensation threshold trigger (occurrenceCount ≥ threshold)
- ❌ No test for the REST API endpoint's lack of dedup/increment
- ❌ No test verifying the orphan branch serialization of `occurrenceCount`
- ❌ No test for concurrent increment race conditions

**Existing test coverage** (`artifacts/api-server/test/integration/generate.test.ts`):

- Tests that `POST /api/projects/:id/generate` creates L1/L2/L3 nodes ✅
- Asserts `l3NodesCreated: 1` ✅
- ❌ No assertion on `occurrenceCount` value after dedup match
- ❌ No second-pass test to verify increment behavior

**Test coverage summary:**

| Test area                                 | Status        |
| ----------------------------------------- | ------------- |
| occurrenceCount starts at 1 on new insert | ❌ Not tested |
| occurrenceCount increments on dedup match | ❌ Not tested |
| Condensation trigger at threshold         | ❌ Not tested |
| Concurrent increment safety               | ❌ Not tested |
| Orphan branch serialization of counter    | ❌ Not tested |
| Intent-router ordering by counter         | ❌ Not tested |

## Summary

| Aspect                                | Status | Details                                                 |
| ------------------------------------- | ------ | ------------------------------------------------------- |
| Schema column definition              | ✅ Yes | `integer NOT NULL DEFAULT 1` in Drizzle + migration SQL |
| Increment in generate pipeline        | ✅ Yes | `generate.ts:990-995` — read current, +1, write back    |
| New node starts at 1                  | ✅ Yes | `generate.ts:1029` sets `occurrenceCount: 1`            |
| Orphan branch serialization           | ✅ Yes | `orphan-branch-writer.ts:62` writes `occurrence_count`  |
| Intent-router ordering                | ✅ Yes | `intent-router.ts:457` orders by `occurrenceCount DESC` |
| Atomic increment / concurrency safety | ⚠️ No  | Read-then-write without transaction or advisory lock    |
| REST API dedup bypass                 | ❌ No  | `POST /l2-nodes/:id/l3-nodes` has no increment logic    |
| Test coverage for increment           | ❌ No  | Zero tests for occurrenceCount increment behavior       |

## Gaps & Recommendations

1. **Concurrency risk (MEDIUM):** The read-then-write pattern at `generate.ts:990-995` is not atomic. If two generate requests process similar L3 candidates concurrently, both could read the same `occurrenceCount` value and both write `n+1`, losing one increment. Consider using `SET occurrenceCount = occurrenceCount + 1` (atomic SQL increment) or wrapping in a serializable transaction with the mutex from 1.4.2.

2. **Test coverage (HIGH):** No tests verify the occurrenceCount increment path. Need at least:
   - Test: second similar embedding (≥0.85) → existing node's `occurrenceCount` increments, no new row created
   - Test: condensation threshold is reached → condensation logic fires
   - Test: `occurrenceCount` defaults to 1 on new insert

3. **REST API bypass (MEDIUM):** Same gap identified in 4.1.1 — the `POST /api/l2-nodes/:id/l3-nodes` endpoint creates L3 nodes without dedup or occurrenceCount management. If this endpoint is used directly (not via the generate pipeline), the occurrenceCount feature is completely bypassed.

4. **Positive signal:** The occurrenceCount is correctly used as a quality signal throughout the system — orphan branch sync, intent-router ordering, and condensation triggering all consume it properly. The increment-on-match design from ADR-009 is faithfully implemented in the generate pipeline.
