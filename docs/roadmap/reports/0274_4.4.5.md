# Verification Report: Item 4.4.5 — Web UI: Misc Pool View + "Associate with Project" Action

**Item ID:** 4.4.5
**Description:** Web UI: Misc Pool view + "Associate with Project" action
**Date:** 2026-06-20
**Verdict:** ⚠️ WARN

## Design Spec References

- ADR-012 Document Misc Pool (`docs/design/09-architectural-decisions.md`, lines 313-317):
  - "When a project manager manually associates a misc pool document with a project (via Web UI), the system: (1) Sets `documents.projectId`, (2) Uses `contentHash` to avoid duplicate processing, (3) Promotes into the project's generate pipeline on next run."
  - Consequence note: "⚠️ Web UI needs a 'Misc Pool' view and a 'Associate with Project' action"
- Prerequisite items: 4.4.1 (nullable projectId), 4.4.2 (contentHash), 4.4.3 (Misc Pool extraction without L1/L2/L3), 4.4.4 (Project association flow)

## Source Files Examined

| File | Purpose |
|------|---------|
| `artifacts/kg-engine/src/pages/documents.tsx` | Frontend: Misc Pool tab + Affiliate dialog |
| `artifacts/api-server/src/routes/documents.ts` | Backend: GET /documents/misc, POST /documents/:id/affiliate, POST /documents |
| `lib/api-spec/openapi.yaml` | OpenAPI spec: /documents/misc, /documents/{id}/affiliate, Document schema |
| `lib/api-client-react/src/generated/api.ts` | Generated hooks: useListMiscDocuments, useAffiliateDocument |
| `lib/db/src/schema/documents.ts` | Drizzle schema: documents table |
| `lib/db/drizzle/*.sql` | Migration SQL: documents table DDL |

## Round 1 — Architecture & Design Review

### What ADR-012 Requires

The Web UI must provide:
1. A "Misc Pool" view showing unaffiliated documents
2. An "Associate with Project" action per document
3. The association flow must set projectId, use contentHash for dedup, and promote to pipeline on next run

### Frontend Implementation (`documents.tsx`)

The Documents page implements a tabbed interface with "Upload" and "Misc Pool" tabs:

**Misc Pool Tab (lines ~382–448):**
- Uses `useListMiscDocuments` generated hook to fetch from `GET /api/documents/misc`
- Displays unaffiliated documents in a list with filename, docType badge, and timestamp
- Shows count badge on the tab trigger when documents exist
- Empty state: "No unaffiliated documents" with Inbox icon
- Error state: "Failed to load misc pool" with AlertCircle icon
- Loading state: spinner with "Loading..." text

**"Associate with Project" Action (lines ~434–442):**
- Each document row has a `Button` with `Link2` icon and "Associate with Project" text
- Clicking opens the affiliate dialog via `openAffiliateDialog(docId)`

**Affiliate Dialog (lines ~452–511):**
- Uses `<Dialog>` component with project selector `<Select>` dropdown
- Lists all projects from `useListProjects` via `normalizeProjects`
- Cancel button (disabled during pending mutation)
- Confirm button calls `handleAffiliate` which invokes `useAffiliateDocument` mutation
- On success: invalidates Misc Pool query, closes dialog, resets state
- On error: shows error message in dialog
- Loading state: spinner + "Associating..." text

### Backend Implementation (`documents.ts`)

**GET /documents/misc (lines 23–37):**
- Queries `documentsTable` WHERE `projectId IS NULL`, ordered by `createdAt`
- Returns documents with ISO-8601 serialized dates

**POST /documents/:id/affiliate (lines 44–88):**
- Validates document ID (must be positive finite number)
- Zod-validates body: `{ projectId: z.number().int().positive() }`
- Verifies project exists (404 if not found)
- Updates document: sets `projectId`, `affiliatedAt: new Date()`, `status: "affiliated"`
- Returns 404 if document not found
- Returns updated document with ISO-8601 serialized dates

### OpenAPI Spec

- `GET /documents/misc` → `operationId: listMiscDocuments`, returns `Document[]`
- `POST /documents/{id}/affiliate` → `operationId: affiliateDocument`, accepts `AffiliateDocumentInput`, returns `Document`
- `Document` schema includes: `contentHash`, `affiliatedAt`, `status` (enum: unaffiliated/affiliated), `projectId` (nullable integer)

### Round 1 Findings

| Aspect | Status | Details |
|--------|--------|---------|
| Misc Pool tab exists | ✅ | Dedicated tab with document list, count badge, empty/error/loading states |
| "Associate with Project" button per document | ✅ | Button with Link2 icon on each document row |
| Project selector dialog | ✅ | Dialog with Select dropdown listing all projects |
| Calls affiliate API mutation | ✅ | `useAffiliateDocument` generated hook |
| Invalidates query on success | ✅ | `queryClient.invalidateQueries({ queryKey: getListMiscDocumentsQueryKey() })` |
| Error handling in dialog | ✅ | Shows error message if mutation fails |
| Loading state during mutation | ✅ | Spinner + "Associating..." text, disabled buttons |
| Backend GET /documents/misc | ✅ | Lists documents WHERE projectId IS NULL |
| Backend POST /documents/:id/affiliate | ✅ | Sets projectId, affiliatedAt, status with Zod validation |
| OpenAPI spec complete | ✅ | Both endpoints documented with correct schemas |
| Generated React Query hooks | ✅ | `useListMiscDocuments`, `useAffiliateDocument` both exist |

**Round 1 Verdict: PASS** — The Web UI provides a complete Misc Pool view and "Associate with Project" action. All frontend and backend components are implemented and correctly wired.

## Round 2 — Code Quality & Security Review

### Frontend Routing Mismatch (Critical)

**Finding: Misc pool upload calls non-existent endpoint.**

In `documents.tsx:118-120`, the `handleIngest` function:
```typescript
const url = selectedProject
  ? `/api/projects/${selectedProject}/ingest/document`
  : `/api/documents/ingest`;
```

When no project is selected (misc pool upload), the frontend calls `POST /api/documents/ingest`. However:
- The backend has **no route** for `POST /documents/ingest` — only `POST /documents` (with `documentUpload.single("file")` middleware)
- The OpenAPI spec has **no `/documents/ingest` endpoint**
- This means misc pool uploads from the Web UI will fail with a 404

**Impact:** Users cannot upload documents to the Misc Pool via the Web UI. The upload tab's "Save to Misc Pool" button is non-functional.

**Fix:** Change the URL from `/api/documents/ingest` to `/api/documents` in `documents.tsx:120`.

### Backend: Missing Authentication on Affiliate Endpoint

The `POST /documents/:id/affiliate` endpoint has no authentication middleware. Any authenticated-or-not caller can associate documents with any project. While this is an internal API, it should at minimum verify the caller has access to the target project.

### Backend: No Authorization Check

The affiliate endpoint verifies the project exists but does not check if the current user owns or has access to the project. This could allow associating documents with arbitrary projects.

### `validityStatus: 'pending_affiliation'` Not in Schema Enum

In `documents.ts:135`, the upload endpoint sets `validityStatus: 'pending_affiliation'`. However:
- The Drizzle schema default is `"active"` — no `'pending_affiliation'` value is defined
- The `status` column (separate from `validityStatus`) defaults to `"unaffiliated"` which is correct
- The `validityStatus` column uses a plain `text` type (no pgEnum), so the value will be accepted at the DB level
- This is inconsistent with the rest of the codebase where `validityStatus` uses `"valid"`, `"pending"`, `"orphaned"` (per ADR-011)

### Round 2 Findings

| Aspect | Status | Details |
|--------|--------|---------|
| Misc pool upload endpoint mismatch | ❌ | Frontend calls `/api/documents/ingest`, backend only has `POST /documents` |
| Auth on affiliate endpoint | ⚠️ | No authentication middleware |
| Authorization on affiliate endpoint | ⚠️ | No project ownership check |
| `validityStatus` value consistency | ⚠️ | `'pending_affiliation'` not aligned with ADR-011 enum values |
| Zod validation on affiliate body | ✅ | `AffiliateBodySchema` validates `projectId` |
| Input sanitization on document ID | ✅ | Checks `Number.isFinite(id) && id <= 0` |
| Error serialization | ✅ | ISO-8601 date serialization in responses |

**Round 2 Verdict: WARN** — The most critical issue is the frontend routing mismatch that makes misc pool uploads non-functional. The auth/authorization gaps are moderate concerns.

## Round 3 — Integration & Completeness Review

### End-to-End Flow Analysis

**Happy Path (Associate existing misc pool document):**
1. User navigates to Documents → Misc Pool tab ✅
2. `useListMiscDocuments` fetches from `GET /api/documents/misc` ✅
3. User clicks "Associate with Project" → dialog opens ✅
4. User selects project and clicks Confirm ✅
5. `useAffiliateDocument` mutation calls `POST /api/documents/:id/affiliate` ✅
6. Backend sets `projectId`, `affiliatedAt`, `status: "affiliated"` ✅
7. Frontend invalidates Misc Pool query → list refreshes ✅
8. Document appears in next generate run (per 4.4.4 verification) ✅

**Broken Path (Upload new document to Misc Pool):**
1. User navigates to Documents → Upload tab ✅
2. User leaves project unselected (goes to Misc Pool) ✅
3. User enters filename and content, clicks "Save to Misc Pool" ✅
4. Frontend calls `POST /api/documents/ingest` ❌ **404 — no such route**
5. Upload fails silently (error caught and displayed)

### Test Coverage

**No test coverage for any misc pool endpoint:**
- No tests for `GET /documents/misc`
- No tests for `POST /documents/:id/affiliate`
- No tests for `POST /documents` (misc pool upload)
- No frontend component tests for the Misc Pool tab or Affiliate dialog

### OpenAPI ↔ Backend Consistency

| Endpoint | OpenAPI | Backend | Match |
|----------|---------|---------|-------|
| GET /documents/misc | ✅ | ✅ | ✅ |
| POST /documents/{id}/affiliate | ✅ | ✅ | ✅ |
| POST /documents (upload) | ❌ Not in spec | ✅ | ❌ |
| POST /documents/ingest | ❌ Not in spec | ❌ Doesn't exist | N/A |

The `POST /documents` upload endpoint exists in the backend but is **not in the OpenAPI spec**. The frontend calls `/documents/ingest` which doesn't exist anywhere.

### Round 3 Findings

| Aspect | Status | Details |
|--------|--------|---------|
| Associate flow end-to-end | ✅ | Complete from UI to DB |
| Upload-to-misc-pool flow | ❌ | Broken — frontend calls non-existent `/api/documents/ingest` |
| POST /documents in OpenAPI spec | ❌ | Missing from spec |
| Test coverage | ❌ | Zero tests for all 3 misc pool endpoints |
| Frontend component tests | ❌ | No snapshot or interaction tests |
| Query invalidation on success | ✅ | Correctly invalidates Misc Pool query |
| Error state handling | ✅ | Both frontend and backend handle errors |

**Round 3 Verdict: WARN** — The associate flow works end-to-end, but the upload flow is broken due to the routing mismatch. No test coverage exists.

## Summary

| Aspect | Status | Details |
|--------|--------|---------|
| Misc Pool tab UI | ✅ Done | Complete with list, count badge, empty/error/loading states |
| "Associate with Project" button | ✅ Done | Per-document button with dialog |
| Affiliate dialog | ✅ Done | Project selector, confirm/cancel, error/loading states |
| Backend GET /documents/misc | ✅ Done | Lists unaffiliated documents |
| Backend POST /documents/:id/affiliate | ✅ Done | Sets projectId, affiliatedAt, status |
| Generated React Query hooks | ✅ Done | useListMiscDocuments, useAffiliateDocument |
| OpenAPI spec for misc endpoints | ✅ Done | Both endpoints documented |
| Upload to Misc Pool via Web UI | ❌ Broken | Frontend calls `/api/documents/ingest`, backend has no such route |
| POST /documents in OpenAPI spec | ❌ Missing | Upload endpoint not in spec |
| Test coverage | ❌ None | No tests for any misc pool endpoint |
| Auth on affiliate endpoint | ⚠️ Missing | No authentication middleware |
| `validityStatus` value consistency | ⚠️ | `'pending_affiliation'` not aligned with ADR-011 |

## Gaps & Recommendations

1. **[CRITICAL] Fix misc pool upload routing.** Change `documents.tsx:120` from `/api/documents/ingest` to `/api/documents`. The backend `POST /documents` endpoint exists with `documentUpload.single("file")` middleware and correctly handles unaffiliated uploads. This is a one-line fix that unblocks the entire Misc Pool upload flow.

2. **[HIGH] Add `POST /documents` to OpenAPI spec.** The upload endpoint exists in the backend but is missing from `openapi.yaml`. Add it under the `documents` tag with `operationId: uploadDocument` and appropriate request/response schemas. Then run Orval codegen to generate the corresponding React Query hook.

3. **[MEDIUM] Add authentication to affiliate endpoint.** The `POST /documents/:id/affiliate` endpoint should require authentication and verify the caller has access to the target project.

4. **[MEDIUM] Align `validityStatus` values.** The `'pending_affiliation'` value set during upload should be reconciled with the ADR-011 validity status enum (`pending`, `valid`, `orphaned`). Consider using `status: 'unaffiliated'` (which already exists) as the sole discriminator and leaving `validityStatus` as the default `"active"`.

5. **[LOW] Add test coverage.** Write integration tests for:
   - `GET /documents/misc` — returns only unaffiliated documents
   - `POST /documents/:id/affiliate` — sets projectId, returns 404 for missing doc/project
   - `POST /documents` — uploads file, computes contentHash, sets status to unaffiliated

## Final Verdict: ⚠️ WARN

The Web UI Misc Pool view and "Associate with Project" action are **fully implemented and functional** — the tab rendering, document list, affiliate dialog, project selector, mutation handling, and query invalidation all work correctly. The backend endpoints for listing and affiliating are also complete with proper validation.

However, the verdict is **WARN** due to one critical gap: the **misc pool upload flow is broken** because the frontend calls `/api/documents/ingest` which doesn't exist on the backend. This means users cannot upload new documents to the Misc Pool via the Web UI — they can only associate existing ones. Additionally, there is no test coverage for any misc pool functionality, and the upload endpoint is missing from the OpenAPI spec.
