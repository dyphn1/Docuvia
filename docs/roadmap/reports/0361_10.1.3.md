# Verification Report: Item 10.1.3 — CI Node.js Version Alignment

**Item ID:** 10.1.3
**Description:** CI runs Node 22, production targets Node 24 (documented discrepancy)
**Date:** 2026-07-04
**Verdict:** PASS

---

## Design Spec References

- `docs/design/02-constraints.md` §2.1 — "Node.js 24 in GitHub Actions (Aligned with production)"
- `docs/design/07-deployment.md` §7.4 — "Node.js: `24.x` (Aligned with production)"
- `docs/design/07-deployment.md` §7.1 — "api-server: Node.js 24 process"
- Checklist item 10.1.3: "CI runs Node 22, production targets Node 24 (documented discrepancy)"

## Source Files Examined

| File | Purpose |
|------|---------|
| `.github/workflows/ci.yml` | CI workflow definition (lint, typecheck-and-build, test jobs) |
| `.github/workflows/release.yml` | Release workflow (VSIX packaging) |
| `package.json` | Root package manifest (packageManager, scripts) |
| `docs/design/02-constraints.md` | Technical constraints specification |
| `docs/design/07-deployment.md` | Deployment view with runtime requirements |
| `[DEPRECATED_V1_DOC]` | Legacy V1 design doc (mentions Node 22) |

---

## Round 1 — Design Doc Claim Verification

### Claim to Verify

Checklist item 10.1.3 states: "CI runs Node 22, production targets Node 24 (documented discrepancy)"

### Investigation

The checklist text implies a discrepancy — that CI runs Node 22 while production requires Node 24. However, the current design specs (`02-constraints.md` and `07-deployment.md`) both explicitly mandate **Node.js 24** in CI, aligned with the production target.

| Source | Statement | Node Version |
|--------|-----------|--------------|
| `02-constraints.md` §2.1 | "Node.js 24 in GitHub Actions (Aligned with production)" | 24 |
| `07-deployment.md` §7.4 | "Node.js: `24.x` (Aligned with production)" | 24 |
| `07-deployment.md` §7.1 | "api-server: Node.js 24 process" | 24 |
| `02-constraints.md` §2.1 | "Runtime: Node.js 24+, TypeScript (strict mode), ESM modules only" | 24+ |
| `archive_v1_design/06-ci-cd-pipeline.md` | "Node 22" (legacy V1 design doc) | 22 (stale) |

**Round 1 Finding:** The current design specs are internally consistent — both production and CI target Node.js 24. The V1 archive doc that mentions Node 22 is stale and predates the final Node 24 decision.

---

## Round 2 — Implementation Reality Check

### Question

Do the actual CI workflow files use Node.js 24 (matching the design spec), or Node 22 (as the checklist discrepancy claim suggests)?

### Evidence from `.github/workflows/ci.yml`

| Job | Node Version Configured | Line Reference |
|-----|------------------------|----------------|
| `lint` | `node-version: "24"` | Line 36 |
| `typecheck-and-build` | `node-version: "24"` | Line 63 |
| `test` | `node-version: "24"` | Line 115 |

### Evidence from `.github/workflows/release.yml`

| Job | Node Version Configured | Line Reference |
|-----|------------------------|----------------|
| `release` | `node-version: "24"` | Line 24 |

### Evidence from `package.json`

- `packageManager: "pnpm@9.15.9"` — compatible with Node 24
- No `engines` field restricting Node version (relies on `02-constraints.md` policy)

### Verification Steps Performed

1. **All three CI jobs** in `ci.yml` use `node-version: "24"` via `actions/setup-node@v4`
2. **The release workflow** also uses `node-version: "24"`
3. **No occurrence of `node-version: "22"`** exists anywhere in `.github/workflows/`
4. **Design specs mandate Node 24** — the implementation matches the spec exactly

**Round 2 Finding:** The CI implementation uses Node.js 24 everywhere. There is zero discrepancy between CI Node version and the production target. The checklist item's claim of "CI runs Node 22" is factually incorrect for the current codebase.

---

## Round 3 — Impact & Risk Assessment

### Discrepancy Analysis

The checklist item was likely generated from an early draft or stale V1 design doc (`06-ci-cd-pipeline.md` in `[DEPRECATED_V1_DOC]` which mentioned Node 22. The project subsequently standardized on Node 24 (as documented in `02-constraints.md` and `07-deployment.md`), but the checklist text was never updated to reflect this resolution.

| Aspect | Checklist Claim | Reality | Match |
|--------|----------------|---------|-------|
| CI Node version | 22 | 24 | ❌ (checklist wrong) |
| Production Node version | 24 | 24 | ✅ |
| Discrepancy exists? | Yes | No | ❌ (resolved) |

### Risks

1. **🟢 INFO — Checklist text is stale/misleading:** The checklist says "CI runs Node 22" but CI actually runs Node 24. This is a documentation inaccuracy, not a code defect. The checklist item should be updated to reflect the resolved state.

2. **🟢 INFO — No actual version mismatch:** There is no runtime risk. CI and production both target Node 24, ensuring consistent behavior between CI and production environments.

3. **🟢 INFO — V1 archive doc is stale:** `[DEPRECATED_V1_DOC]` still mentions Node 22, but this is an archived historical document and does not affect current implementation.

### Positive Findings

1. ✅ **Full Node version alignment** — CI (all 3 jobs) and release workflow use Node 24, matching production target
2. ✅ **Design specs are internally consistent** — `02-constraints.md` and `07-deployment.md` both mandate Node 24
3. ✅ **No version drift** — All workflow files use the same Node version
4. ✅ **Actions versions are current** — `actions/setup-node@v4`, `pnpm/action-setup@v4`

---

## Findings Summary

| # | Severity | Finding |
|---|----------|---------|
| 1 | 🟢 Info | Checklist text claims "CI runs Node 22" but CI actually runs Node 24 — the checklist description is stale |
| 2 | 🟢 Info | V1 archive design doc (`06-ci-cd-pipeline.md`) mentions Node 22 but is superseded by current specs |
| 3 | ✅ Good | All CI jobs and release workflow use Node 24, fully aligned with production target |
| 4 | ✅ Good | Design specs (`02-constraints.md`, `07-deployment.md`) are internally consistent on Node 24 |

---

## Recommendations

1. **[LOW] Update checklist item 10.1.3 text** — Change from "CI runs Node 22, production targets Node 24 (documented discrepancy)" to "CI runs Node 24, production targets Node 24 (aligned)" to reflect reality.

2. **[LOW] Add `engines` field to root `package.json`** — Consider adding `"engines": { "node": ">=24" }` to enforce the Node 24 requirement at install time. This is a belt-and-suspenders approach.

3. **[INFO] Archive doc staleness is acceptable** — The V1 design doc in `[DEPRECATED_V1_DOC]` is a historical record. No action needed unless it's referenced by active tooling.

---

## Overall Verdict

### **PASS** — No discrepancy exists; CI and production both use Node.js 24

The checklist item's claim of a "documented discrepancy" between CI (Node 22) and production (Node 24) is **stale and factually incorrect**. The actual implementation shows:
- All three CI jobs (`lint`, `typecheck-and-build`, `test`) use `node-version: "24"`
- The release workflow uses `node-version: "24"`
- Design specs (`02-constraints.md`, `07-deployment.md`) mandate Node 24 for both CI and production

The "discrepancy" referenced in the checklist has been resolved — the project standardized on Node 24 across the board. The checklist text itself needs updating to reflect this, but the code and configuration are correct.

### Status for Tracking Table

**PASS** — CI runs Node 24, production targets Node 24. Full alignment, no discrepancy.
