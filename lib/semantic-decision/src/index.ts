import "./register.js";

/**
 * Intentionally no model/runtime exports.
 *
 * The public package entrypoint exists for composition-root self-registration only. Docuvia
 * consumers use @workspace/contracts and TOKENS.SemanticDecisionProvider.
 */
export {};
