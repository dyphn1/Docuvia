--- lib/core/src/services/swarm/llm-rate-limiter.ts
+++ lib/core/src/services/swarm/llm-rate-limiter.ts
@@ -1,5 +1,14 @@
-import { isRateLimitError } from "@workspace/integrations-openai-ai-server";
+function isRateLimitError(error: unknown): boolean {
+  if (error && typeof error === 'object') {
+    if ('status' in error && (error as any).status === 429) {
+      return true;
+    }
+    if (error instanceof Error && error.message.includes('status 429')) {
+      return true;
+    }
+  }
+  return false;
+}
 
 const sleep = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));
